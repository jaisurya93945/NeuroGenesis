name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      # torch is deliberately NOT installed here: the oracle gates -- the tests that
      # actually protect the science -- must pass without it, and the CUDA wheel is ~4 GB.
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Lint
        run: ruff check src tests scripts experiments
      - name: Format check
        run: ruff format --check src tests scripts experiments
      - name: Oracle correctness gates
        run: pytest tests/test_oracle_analytic.py -q
      # Bare `pytest`, deliberately: unlike `python -m pytest` it does NOT put the
      # working directory on sys.path, so this catches any test that depends on
      # cwd-relative imports. One such import broke CI while every local run passed.
      - name: Full suite (excluding tests needing MNIST or torch)
        run: pytest tests/ -q -m "not needs_mnist and not needs_torch"
