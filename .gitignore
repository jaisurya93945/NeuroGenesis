__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Raw data is downloadable and checksum-verified; never committed
data/raw/

# Run records and figures ARE committed while they stay small (see results/README.md).
# Only oversized sweep artefacts are excluded.
results/runs/*.tmp
results/**/*.npz.large
